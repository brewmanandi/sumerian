{
  "name": "amplify-js-app",
  "version": "1.0.0",
  "description": "AWS Amplify JavaScript Example",
  "dependencies": {
    "aws-amplify": "^1.1.19"
  },
  "devDependencies": {
    "webpack": "^4.17.1",
    "webpack-cli": "^3.3.12",
    "copy-webpack-plugin": "^4.5.2",
    "webpack-dev-server": "^3.1.5"
  },
  "scripts": {
    "start": "webpack && cd serve && npm install && cd .. && ./serve/serve -d dist -n",
    "start-windows": "webpack && pushd serve && npm install && popd && serve\\bin\\serve.bat -d dist -n",
    "build": "webpack"
  }
}
